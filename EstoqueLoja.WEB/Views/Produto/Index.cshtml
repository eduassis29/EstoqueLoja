@model IEnumerable<EstoqueLoja.WEB.Models.Produto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomePro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PrecoPro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomePro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrecoPro)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdPro }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdPro }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdPro })
            </td>
        </tr>
}
    </tbody>
</table>
